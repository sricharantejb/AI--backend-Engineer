# BUILDLOG

AI assistance was used to scaffold the application, routes, validation, documentation and tests. Human verification is required before submission; record corrections and test results here.

## Simplifications
SQLite is used for local zero-setup development. Authentication uses a local demo bearer token and should be replaced with a real identity provider for production. Geo and side effects are intentionally resilient.
